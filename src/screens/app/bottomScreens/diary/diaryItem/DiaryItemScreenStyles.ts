import { normalize } from '@utils'
import styled from 'styled-components/native'

export const DiaryItemScreenContainer = styled.View`
  width: 100%;
  height: ${normalize(300)}px;
  background-color: red;
`

export const DiaryItemScreenSpace = styled.View`
  height: ${normalize(24)}px;
`
